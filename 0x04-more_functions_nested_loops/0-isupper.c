#include "main.h"
#include <stdio.h>

/**
* _isupper - takes a date and prints how many days are
* @c: letter to check
* Return: 1 if uppercase, 0 if otherwise
*/

int _isupper(int c)
{
int c;
if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
