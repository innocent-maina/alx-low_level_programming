#include "main.h"

/**
* _isdigit - takes a date and prints how many days are
* @c: letter to check
* Return: 1 if digit, 0 if otherwise
*/

int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}
else
{
return (0);
}
}
